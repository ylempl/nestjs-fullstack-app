import { User } from './User';
import { Session } from './Session';
import { Product } from './Product';
const entities = [User, Session, Product];

export { User, Session, Product };

export default entities;
